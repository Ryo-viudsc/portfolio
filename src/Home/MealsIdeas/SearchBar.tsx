import React from "react";
import { ScrollView, Dimensions, StyleSheet, Text, View } from "react-native";

//to do
//1, the button to change the order of the list 
//newer older alphabet etc 
//make the modal components to filter out the receipe list 
//just the list showing 
//old to new , new to old
//alphabetical order 

//2, make the callback function in the LikedMeals component 
//based on what is chosen,
//sort the array of strings and show the new ordered list 


interface SearchBarProps 
{
    callback : ()=>{};
}


const SearchBar = () => {




  return (
    <View> 
    </View>
  );
}




export default SearchBar;